# Shell Aliases

Managed via Ansible in `roles/settings/tasks/main.yml` – written to `~/.zshrc`.

## Git

| Alias | Command |
|-------|---------|
| `gs` | `git status` |

## Docker

| Alias | Command |
|-------|---------|
| `dps` | `docker ps -a` |
| `dlogs` | `docker logs` |
| `dinspect` | `docker inspect` |

## Claude Code

| Alias | Command |
|-------|---------|
| `cld` | `claude` |

## Terminal

| Alias | Command |
|-------|---------|
| `cl` | `clear` |
